# المساهمة في سكينة

المشروع خاص، لكن ده الدليل المتبع لأي تعديل جوه الفريق أو بواسطة متعاون مصرّح له.

## قبل ما تبدأ

```bash
npm install
npx expo start
```

راجع `AGENTS.md` — فيه ملاحظة مهمة إن Expo بيتغيّر بسرعة، فلازم تتأكد من التوثيق الرسمي بتاع النسخة المستخدمة (`docs.expo.dev/versions/v54.0.0`) قبل ما تكتب أي كود بيعتمد على API معيّن.

## قواعد الكود

- **TypeScript صارم** — من غير `any` إلا لو مفيش بديل، ووقتها اكتب تعليق يوضح ليه.
- **الألوان** كلها بتتسحب من `constants/theme.ts` عن طريق `useAppTheme()` — من غير هيكس مباشر جوه `StyleSheet.create`.
- **التخزين المحلي** كل مفتاح AsyncStorage له اسم واضح ومنطقي (`sakeenah_<feature>_<detail>`)، وأي دالة بتقرأ/تكتب فيه لازم تكون في `try/catch` وترجع قيمة افتراضية آمنة بدل ما تكسر الشاشة.
- **قنوات الإشعارات على أندرويد** — لو غيّرت خصائص قناة إشعار موجودة (صوت، اهتزاز)، لازم تعمل قناة بـ ID جديد (`-v2`, `-v3`...) لأن أندرويد مش بيحدّث القنوات الموجودة تلقائيًا.

## قبل ما تعمل PR

```bash
npm run lint
npm run typecheck
npm run format:check
npm test
```

الأربعة دول بيتشغلوا تلقائيًا على GitHub Actions مع كل push/PR، لكن شغّلهم محليًا الأول عشان توفر وقت الـ review.

## الاختبارات

- أي منطق حسابي أو تحويل بيانات (تواريخ، حسابات فلكية، streaks) لازم يبقى له اختبار Jest في `__tests__/`.
- الدوال اللي بتعتمد على `new Date()` من غير ما تقبل مرجع زمني كـ parameter، استخدم `jest.useFakeTimers().setSystemTime(...)` بدل ما تغيّر توقيت الجهاز الحقيقي.
- لو دالة داخلية (مش exported) محتاجة اختبار مباشر، اعملها زي `__testables` الموجودة في `data/streak.ts` بدل ما تصدّرها بشكل عام.

## الرسائل والنصوص

كل نص ظاهر للمستخدم بالعربي المصري البسيط، من غير مصطلحات إنجليزية إلا لو مفيش مقابل واضح. الرسائل التقنية (تعليقات الكود، أسماء المتغيرات) بالإنجليزي.

## الإبلاغ عن مشاكل

افتح Issue فيه:

1. وصف المشكلة والخطوات لتكرارها
2. الـ platform (iOS/Android/web) والنسخة
3. لو فيه كراش، أي سجل خطأ متاح (شوف `lib/errorReporting.ts` لآخر الأخطاء المسجّلة محليًا على الجهاز)
